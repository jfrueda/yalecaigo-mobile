# Android recuperado parcialmente

Estos son los únicos archivos no vacíos y potencialmente útiles de la carpeta
Android recibida. Se conservan como evidencia, no como plataforma compilable.

El paquete Kotlin recuperado es:

```text
opentic.co.yalecaigo
```

Ejecuta `../../scripts/reconstruir_android.sh` desde la raíz del frontend para
generar una plataforma Android válida.
